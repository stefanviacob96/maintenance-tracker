apiVersion: v1
kind: Service
metadata:
  name: personal-tracker-frontend
spec:
  type: NodePort
  selector:
    app: personal-tracker-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
